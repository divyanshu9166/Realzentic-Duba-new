import { notFound } from 'next/navigation';

export const metadata = {
    title: 'Leaderboard | Realzentic',
    description: 'Team performance analytics are available from the dashboard.',
};

export default function LeaderboardPage() {
    notFound();
}
