'use client'

import { useCallback, useEffect, useState, type FormEvent } from 'react'
import { ClipboardCheck, Plus, RefreshCw, Wrench } from 'lucide-react'
import { assignWorkOrder, createWorkOrder, getWorkOrderWorkspace, linkExpenseToWorkOrder, updateWorkOrderStatus } from '@/app/actions/work-orders'

export default function MaintenanceClient() {
  const [data, setData] = useState<any>({ orders: [], staff: [], leases: [] })
  const [message, setMessage] = useState('')
  const [form, setForm] = useState<any>({ title: '', description: '', category: 'GENERAL', priority: 'MEDIUM', assignedToId: '', leaseId: '', dueAt: '', estimatedCost: 0 })
  const [expenseForm, setExpenseForm] = useState({ expenseId: '', workOrderId: '' })
  const load = useCallback(async () => { const result = await getWorkOrderWorkspace(); if (result.success) setData(result.data) }, [])
  // Initial data hydration is an intentional async server-action boundary.
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { load() }, [load])
  async function submit(event: FormEvent) { event.preventDefault(); const result = await createWorkOrder({ ...form, assignedToId: form.assignedToId ? Number(form.assignedToId) : null, leaseId: form.leaseId ? Number(form.leaseId) : null, estimatedCost: Number(form.estimatedCost) }); setMessage(result.success ? 'Work order created.' : result.error ?? 'Could not create work order'); if (result.success) { setForm({ ...form, title: '', description: '' }); await load() } }
  async function status(id: number, next: string) { const result = await updateWorkOrderStatus({ id, status: next }); setMessage(result.success ? 'Work order updated.' : result.error ?? 'Could not update work order'); await load() }
  async function assign(id: number, staffId: string) { const result = await assignWorkOrder(id, staffId ? Number(staffId) : null); setMessage(result.success ? 'Assignment updated.' : result.error ?? 'Could not assign work order'); await load() }
  async function linkExpense(event: FormEvent) { event.preventDefault(); const result = await linkExpenseToWorkOrder(Number(expenseForm.expenseId), Number(expenseForm.workOrderId)); setMessage(result.success ? 'Expense linked and actual cost recalculated.' : result.error ?? 'Could not link expense'); if (result.success) setExpenseForm({ expenseId: '', workOrderId: '' }); await load() }
  return <main className="p-4 md:p-8 space-y-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs uppercase tracking-[0.2em] text-indigo-500">Facilities operations</p><h1 className="mt-1 text-3xl font-semibold">Maintenance Work Orders</h1><p className="mt-2 text-sm text-muted">Schedule, assign and cost repairs across managed leases and bookings.</p></div><button onClick={load} className="rounded-lg border border-border p-2"><RefreshCw className="h-4 w-4" /></button></div>{message && <div className="rounded-lg border border-indigo-200 bg-indigo-50 px-4 py-3 text-sm text-indigo-800">{message}</div>}
    <section className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_minmax(0,1.3fr)]"><form onSubmit={submit} className="rounded-xl border border-border bg-card p-5 space-y-3"><h2 className="flex items-center gap-2 font-semibold"><Plus className="h-5 w-5 text-indigo-500" /> New work order</h2><input required placeholder="Issue title" value={form.title} onChange={e => setForm({ ...form, title: e.target.value })} className="field" /><textarea placeholder="Description / access notes" value={form.description} onChange={e => setForm({ ...form, description: e.target.value })} className="field min-h-24" /><div className="grid gap-3 sm:grid-cols-2"><select value={form.category} onChange={e => setForm({ ...form, category: e.target.value })} className="field"><option value="GENERAL">General</option><option value="PLUMBING">Plumbing</option><option value="ELECTRICAL">Electrical</option><option value="HVAC">HVAC</option><option value="CIVIL">Civil</option><option value="CLEANING">Cleaning</option></select><select value={form.priority} onChange={e => setForm({ ...form, priority: e.target.value })} className="field"><option value="LOW">Low</option><option value="MEDIUM">Medium</option><option value="HIGH">High</option><option value="URGENT">Urgent</option></select><select value={form.assignedToId} onChange={e => setForm({ ...form, assignedToId: e.target.value })} className="field"><option value="">Assign staff</option>{data.staff.map((item: any) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select value={form.leaseId} onChange={e => setForm({ ...form, leaseId: e.target.value })} className="field"><option value="">Lease (optional)</option>{data.leases.map((item: any) => <option key={item.id} value={item.id}>{item.contractNumber} · {item.contact.name}</option>)}</select><input type="datetime-local" value={form.dueAt} onChange={e => setForm({ ...form, dueAt: e.target.value })} className="field" /><input type="number" min="0" placeholder="Estimated cost (AED)" value={form.estimatedCost} onChange={e => setForm({ ...form, estimatedCost: e.target.value })} className="field" /></div><button className="w-full rounded-lg bg-indigo-600 px-4 py-2.5 text-sm font-medium text-white">Create work order</button></form><form onSubmit={linkExpense} className="rounded-xl border border-border bg-card p-5 space-y-3"><h2 className="font-semibold">Link expense to completed work</h2><p className="text-sm text-muted">Create the expense in Expenses first, then link its ID here. The work order actual cost is recalculated from linked expenses.</p><div className="grid gap-3 sm:grid-cols-2"><input required type="number" min="1" placeholder="Expense ID" value={expenseForm.expenseId} onChange={e => setExpenseForm({ ...expenseForm, expenseId: e.target.value })} className="field" /><select required value={expenseForm.workOrderId} onChange={e => setExpenseForm({ ...expenseForm, workOrderId: e.target.value })} className="field"><option value="">Select work order</option>{data.orders.map((item: any) => <option key={item.id} value={item.id}>{item.displayId} · {item.title}</option>)}</select></div><button className="rounded-lg border border-border px-4 py-2.5 text-sm">Link and recalculate</button></form></section>
    <section className="rounded-xl border border-border bg-card p-5"><div className="mb-4 flex items-center gap-2"><Wrench className="h-5 w-5 text-indigo-500" /><h2 className="font-semibold">Work-order register</h2></div><div className="space-y-3">{data.orders.map((order: any) => <article key={order.id} className="rounded-lg border border-border p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><span className="font-medium">{order.displayId}</span><span className="rounded-full bg-muted/15 px-2 py-1 text-xs">{order.priority}</span><span className="rounded-full bg-indigo-50 px-2 py-1 text-xs text-indigo-700">{order.status}</span></div><p className="mt-2 text-sm">{order.title} · {order.category}</p><p className="mt-1 text-xs text-muted">{order.leaseNumber ? `Lease ${order.leaseNumber}` : 'General maintenance'} · Estimated AED {order.estimatedCost.toLocaleString()} · Actual AED {order.actualCost.toLocaleString()}</p></div><div className="flex flex-wrap gap-2"><select value={order.assignedToId ?? ''} onChange={e => assign(order.id, e.target.value)} className="field min-w-[150px]"><option value="">Unassigned</option>{data.staff.map((item: any) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><select value={order.status} onChange={e => status(order.id, e.target.value)} className="field min-w-[140px]"><option value="OPEN">Open</option><option value="SCHEDULED">Scheduled</option><option value="IN_PROGRESS">In progress</option><option value="COMPLETED">Completed</option><option value="CANCELLED">Cancelled</option></select></div></div></article>)}{data.orders.length === 0 && <div className="py-8 text-center text-sm text-muted"><ClipboardCheck className="mx-auto mb-2 h-8 w-8" />No work orders yet.</div>}</div></section>
  </main>
}
